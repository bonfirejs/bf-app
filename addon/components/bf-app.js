import Ember from 'ember';
import layout from '../templates/components/bf-app';

export default Ember.Component.extend({
  layout: layout,
  classNames: ['wrapper', 'skin-blue', 'layout-top-nav']
});
